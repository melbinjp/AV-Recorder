{
  "name": "av-recorder-tests",
  "private": true,
  "description": "Browser tests for AV Recorder. The app itself needs no install; this is only for developers and CI.",
  "type": "module",
  "scripts": {
    "test": "node --test --test-concurrency=1 --test-timeout=900000 *.test.mjs"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
